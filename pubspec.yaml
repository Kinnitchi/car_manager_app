name: car_manager_app
description: "A new Flutter project."
version: 1.0.0+1

environment:
  sdk: ^3.12.2
dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0

  # --- Isar Community (fork mantido, com fix de namespace) ---
  isar_community: ^3.3.2
  isar_community_flutter_libs: ^3.3.2
  permission_handler: ^11.3.1
  path_provider: ^2.1.3
  fl_chart: ^0.68.0
  intl: ^0.19.0
  image_picker: ^1.1.2
  flutter_local_notifications: ^17.2.2
  timezone: ^0.9.4
  cupertino_icons: ^1.0.6

  # --- UI / design system ---
  google_fonts: ^6.2.1
  shimmer: ^3.0.0
  flutter_animate: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.11
  isar_community_generator: ^3.3.2

flutter:
  uses-material-design: true
